#include <bits/stdc++.h>
using namespace std;
char a[1<<20];
string x;
int lew, prw;
bool r;
int main()
{
    ios_base::sync_with_stdio(false); cin.tie(0); cout.tie(0);
    cin>>x;
    for(auto i : x)
    {
        if(i == '(' and r=true) 
    }
    return 0;
}