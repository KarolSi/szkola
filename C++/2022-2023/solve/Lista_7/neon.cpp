#include <bits/stdc++.h>
using namespace std;
const int SIZE = 1e9+10;
int n,m,q;
int w[SIZE];
int v[SIZE];

int main()
{
    ios_base::sync_with_stdio(false); cin.tie(0); cout.tie(0);
    cin>>n;
    for (int i = 0; i < n; i++)
        cin>>w[i];

    
    cin>>m;
    for(int i = 0; i < m; i++)
        cin>>v[i];

    cin>>q;
    
    
    return 0;
}